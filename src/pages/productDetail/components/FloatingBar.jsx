import React from 'react';

const FloatingBar = () => {
    return <div style={{padding: '50px', background: '#f5f5f5', border: '1px solid #ddd'}}>FloatingBar 영역입니다!</div>;
};

export default FloatingBar;