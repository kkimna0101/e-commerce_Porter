import React from 'react';

const FloatingBar = () => {
    return (
        <div className="pd-section-placeholder">
            FloatingBar 영역입니다!
        </div>
    );
};

export default FloatingBar;